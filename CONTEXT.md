# Slab 领域词汇表

通用 ERP 后端（采购 → 质检 → 销售 → 生产 → 财务），DDD 垂直切片。本表是统一语言：代码、文档、讨论中一律使用本表术语。

## 单据与状态

**单据（Document）**：
采购订单、销售订单、调拨单、盘点单、工单、检验单等带状态流转的业务对象统称。
_Avoid_: 单、表单、业务单

**审批流状态（Approval State）**：
单据在「提交 → 逐级审批 → 批准 / 驳回」时间线上的位置（草稿 / 已提交 / 主管审批 / 已批准 / 已驳回）。
_Avoid_: 状态、流程状态、status

**生命周期状态（Lifecycle State）**：
单据在其自身业务进程中的位置（草稿 / 已下达 / 进行中 / 已完成 / 已关闭），与审批流状态是两条独立时间线。
_Avoid_: 状态、审批状态

**提交（Submit）**：
把草稿单据送入审批流的动作；只有草稿可提交。

**批准（Approve）**：
审批人放行单据的动作，记录批准时间。

**驳回（Reject）**：
审批人退回单据的动作；仅审批中的单据可驳回。

**完成（Complete）**：
生命周期终态——单据走完其业务进程。注意：检验单的「流程走完」不叫完成，见「检验结论」。
_Avoid_: 完结、结束（与「审批通过」区分）

**下达（Release）**：
工单从草稿转为可执行状态（已下达）的动作。

**发布（Publish）**：
BOM 从草稿转为已发布状态的动作；只有已发布的 BOM 会被 MRP 纳入需求计算。
_Avoid_: 下达——「下达」专指工单，BOM 用「发布」

## 库存

**台账操作（Ledger Operation）**：
一次原子库存变动，同时更新库存量与流水记录。

**入库（Receive）**：
增加指定仓库的库存。

**出库（Issue）**：
减少指定仓库的库存；库存不足时拒绝。

**负库存出库（Negative Issue）**：
允许库存变为负数的出库，用于退货等场景。
_Avoid_: 强制出库——「强制」暗示绕过检查，与真实语义不符

**盘点调整（Adjust）**：
将库存直接设为指定数量（盘点、期初建账）。

**调拨（Transfer）**：
从源仓库出库 + 目标仓库入库的成对操作。

## 检验

**检验单状态（Inspection Order State）**：
检验单在流程中的位置（待检 → 已检）。「已检」不代表通过。

**检验结论（Verdict）**：
检验人对检验单下达的判定：通过（Pass）/ 不通过（Fail）/ 有条件通过（Conditional）。
_Avoid_: 完成、结果——结论与检验单状态是两个独立维度；不合格的检验单同样走完流程（状态=已检），只是结论=不通过

## 财务

**收付款（Payment）**：
针对销售发票（收款）或采购发票（付款）的资金记录。
_Avoid_: 收款单、付款单

**财务期间（Fiscal Period）**：
年 + 月份范围，报表统计的时间窗口。
