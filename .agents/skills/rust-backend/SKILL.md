---
name: rust-backend
description: "Slab Rust 垂直切片细节。仅当新建域、新加 endpoint 文件、或架构落位不确定时读取 docs/ai/backend.md；改已有 endpoint 的局部实现不要加载。"
---

# rust-backend

**Trigger**: 新建域、新加 endpoint 文件、架构落位不确定。

不要把本文件当百科。导航 / 打开集在根目录 `AGENTS.md`；打开后端文件时编码 / 测试摘要由 `.cursor/rules/backend.mdc` 注入；落位 / 菜谱 / 队列 / KV 在 `docs/ai/backend.md`；编码细则在 `docs/ai/conventions.md`。只打开对应小节。

改已有 endpoint 的 `execute` / handler / 测试且不改 contract：到此停止，不要继续读。
